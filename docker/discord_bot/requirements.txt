discord.py[voice]>=2.6.4
python-dotenv
schedule
requests
beautifulsoup4
lxml
aiohttp
html2text
fake-useragent
pydantic>=2.7,<3
pydantic-settings>=2.2,<3
# === DB & ORM ===
# 用於精確定義資料表，確保歷史紀錄的可觀測性
SQLAlchemy>=2.0.0
# PostgreSQL 的同步與非同步驅動程式
psycopg2-binary
asyncpg
# 讓 SQLAlchemy 支援 PostgreSQL 的向量擴充
pgvector

# === LLM client（OpenAI dialect；對 Ollama / LM Studio / Lemonade / vLLM 等相容後端通用）===
# 自寫 wire 薄殼（src/llm/llm_http_client.py），用 httpx 直接打 /v1/chat/completions、
# /v1/embeddings 與 /v1/models。不依賴任何 LLM 廠商 SDK（openai 早期 v0→v1 大改、
# Pydantic v1→v2 等都炸過 caller），也避開 LlamaIndex provider 子套件硬塞驗證
# （如 llama-index-embeddings-openai 的 OpenAIEmbeddingModelType enum 白名單地雷）。
# wire 規格 2023 年定下來後沒有破壞性更動，是事實標準；綁協定不綁 SDK。
httpx

# === LlamaIndex ===
# LlamaIndex 核心框架（BaseEmbedding 抽象介面、VectorStoreIndex、QueryEngine）。
# 故意不用 llama-index-embeddings-* 系列：provider 子套件品質不齊、常硬塞驗證；
# 改在 src/llm/safe_llm_embedding.py 直接 subclass BaseEmbedding 自己呼叫 wire。
llama-index-core
# 讓 LlamaIndex 能夠讀寫 pgvector 資料庫
llama-index-vector-stores-postgres
# 關鍵字檢索
rank-bm25

# === Music Bot ===
# 包含 ejs + 2026 YouTube 支援
yt-dlp[default]
# discord.py voice 必要
PyNaCl>=1.5.0
# DAVE 加密協定（2026-03 Discord 強制要求，解決 4017）
davey>=0.1.0
